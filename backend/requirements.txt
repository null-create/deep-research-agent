aiohttp
anyio
art
asyncio
black
boto3
neo4j
httpx
fastapi
mcp
ollama
openai
pydantic
# Sentence-transformers is installed separately in the Dockerfile to pre-download the embedding model and cache it in the image, 
# avoiding cold-start latency and unauthenticated HF rate-limit throttling.  If installing locally outside Docker, run:
# sentence-transformers
textual
websockets
# torch is installed by the Dockerfile with the CPU-only index URL to avoid
# pulling the 1.7 GB CUDA build.  If installing locally outside Docker, run:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
